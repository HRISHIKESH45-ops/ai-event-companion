# Optional: If you want to bypass the "Bring Your Own Key" UI and hardcode a server key for local development.
# The application will prioritize local storage keys first, then fallback to this.
GEMINI_API_KEY=your_google_ai_studio_api_key_here
